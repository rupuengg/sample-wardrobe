export interface ISize {
  width: number,
  height: number,
  depth: number,
}

export const defaultSize: ISize = {
  width: 36,
  height: 84,
  depth: 24,
}

export const defaultDrawerSize: ISize = {
  width: 36,
  height: 10,
  depth: 24,
}